---
import { Image } from "astro:assets";
import meImage from "../assets/me.webp";
---

<section id="about-me" class="section-py md:py-32">
  <div class="wrapper">
    <div class="flex flex-col-reverse md:flex-row items-center gap-10">
      <div class="flex-1 text-center md:text-left">
        <h1 class="text-4xl md:text-6xl font-extrabold mb-6 tracking-tight">
          Hola, soy <br class="hidden md:block" />
          <span class="text-accent typed-text"></span>
        </h1>

        <p
          class="text-lg text-slate-600 dark:text-slate-400 mb-8 max-w-lg mx-auto md:mx-0"
        >
          Ingeniero de Software especializado en Frontend. Creo experiencias
          digitales fluidas, accesibles y minimalistas.
        </p>

        <div class="flex justify-center md:justify-start gap-4">
          <a href="/cv_es.pdf" target="_blank" class="btn-primary"
            >Descargar CV</a
          >
          <a href="#contacto" class="btn-secondary">Contactar</a>
        </div>
      </div>

      <div class="flex-1 flex justify-center md:justify-end relative">
        <div
          class="absolute inset-0 bg-linear-to-tr from-blue-600 to-purple-500 rounded-full blur-3xl opacity-20 animate-pulse"
        >
        </div>

        <div
          class="relative w-64 h-64 md:w-80 md:h-80 rounded-full border-4 border-white dark:border-slate-900 shadow-2xl overflow-hidden"
        >
          <Image
            src={meImage}
            alt="Alexis Garcia Foto de Perfil"
            class="w-full h-full object-cover"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import Typed from "typed.js";
  const element = document.querySelector(".typed-text");
  if (element) {
    new Typed(element, {
      strings: ["Alexis Garcia", "Desarrollador", "Frontend Dev"],
      typeSpeed: 50,
      backSpeed: 30,
      backDelay: 1500,
      loop: true,
    });
  }
</script>
